/// <reference types="node" />
import BN from "bn.js";
import { AccURL } from "../acc-url";
import { BasePayload } from "./base-payload";
export declare type CreateTokenArg = {
    url: string | AccURL;
    symbol: string;
    precision: number;
    properties?: string | AccURL;
    supplyLimit?: number | BN | string;
    authorities?: (string | AccURL)[];
};
export declare class CreateToken extends BasePayload {
    private readonly _url;
    private readonly _symbol;
    private readonly _precision;
    private readonly _properties?;
    private readonly _supplyLimit?;
    private readonly _authorities;
    constructor(arg: CreateTokenArg);
    protected _marshalBinary(): Buffer;
}

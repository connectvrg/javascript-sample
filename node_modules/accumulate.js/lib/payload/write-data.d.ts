/// <reference types="node" />
import { BasePayload } from "./base-payload";
export declare type WriteDataArg = {
    data: Uint8Array[];
    scratch?: boolean;
};
export declare class WriteData extends BasePayload {
    private readonly _data;
    private readonly _scratch;
    private _dataHash?;
    constructor(arg: WriteDataArg);
    protected _marshalBinary(): Buffer;
    hash(): Buffer;
}

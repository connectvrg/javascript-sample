/// <reference types="node" />
import BN from "bn.js";
import { AccURL } from "../acc-url";
import { BasePayload } from "./base-payload";
export declare type AddCreditsArg = {
    recipient: string | AccURL;
    amount: number | BN | string;
    oracle: number | BN | string;
};
export declare class AddCredits extends BasePayload {
    private readonly _recipient;
    private readonly _amount;
    private readonly _oracle;
    constructor(arg: AddCreditsArg);
    protected _marshalBinary(): Buffer;
}

/// <reference types="node" />
import { AccURL } from "../acc-url";
import { BasePayload } from "./base-payload";
export declare type CreateDataAccountArg = {
    url: string | AccURL;
    scratch?: boolean;
    authorities?: (string | AccURL)[];
};
export declare class CreateDataAccount extends BasePayload {
    private readonly _url;
    private readonly _scratch;
    private readonly _authorities;
    constructor(arg: CreateDataAccountArg);
    protected _marshalBinary(): Buffer;
}

/// <reference types="node" />
import { AccURL } from "../acc-url";
import { BasePayload } from "./base-payload";
export declare type CreateTokenAccountArg = {
    url: string | AccURL;
    tokenUrl: string | AccURL;
    scratch?: boolean;
    authorities?: (string | AccURL)[];
};
export declare class CreateTokenAccount extends BasePayload {
    private readonly _url;
    private readonly _tokenUrl;
    private readonly _scratch;
    private readonly _authorities;
    constructor(arg: CreateTokenAccountArg);
    protected _marshalBinary(): Buffer;
}

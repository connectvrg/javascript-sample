/// <reference types="node" />
import { AccURL } from "../acc-url";
import { BasePayload } from "./base-payload";
export declare type CreateKeyBookArg = {
    url: string | AccURL;
    publicKeyHash: Uint8Array;
    authorities?: (string | AccURL)[];
};
export declare class CreateKeyBook extends BasePayload {
    private readonly _url;
    private readonly _publicKeyHash;
    private readonly _authorities;
    constructor(arg: CreateKeyBookArg);
    protected _marshalBinary(): Buffer;
}

/// <reference types="node" />
import { AccURL } from "../acc-url";
import { BasePayload } from "./base-payload";
export declare type CreateIdentityArg = {
    url: string | AccURL;
    keyHash?: Uint8Array;
    keyBookUrl?: string | AccURL;
    authorities?: (string | AccURL)[];
};
export declare class CreateIdentity extends BasePayload {
    private readonly _url;
    private readonly _keyHash?;
    private readonly _keyBookUrl?;
    private readonly _authorities;
    constructor(arg: CreateIdentityArg);
    protected _marshalBinary(): Buffer;
}

/// <reference types="node" />
import { Payload } from "../payload";
export declare abstract class BasePayload implements Payload {
    private _binary?;
    private _payloadHash?;
    marshalBinary(): Buffer;
    hash(): Buffer;
    protected abstract _marshalBinary(): Buffer;
}
